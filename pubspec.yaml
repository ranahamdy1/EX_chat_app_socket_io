name: chat_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  socket_io_client: ^2.0.3+1
  # باكدج بسيطة بتنبهك لأخطاء بتعملها في الكود وتقولك تصلحها إزاي.. بحيث ان الكود يبقى منظم وقراءته سهلة
  #we run (dart fix --apply) in terminal
  lint: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true